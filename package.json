{
  "name": "csc-308-app",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "npm run dev -w packages/react-frontend",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Kyle Fan",
  "license": "ISC",
  "description": "",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
